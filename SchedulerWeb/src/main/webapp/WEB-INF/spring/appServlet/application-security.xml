<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security-3.1.xsd">

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="*" access="isAnonymous()" />
	<!-- 
		<intercept-url pattern="/" access="isAnonymous()" />
		<intercept-url pattern="/login" access="isAnonymous()" />
		<intercept-url pattern="/client/login" access="isAnonymous()" />
		<intercept-url pattern="/official/login" access="isAnonymous()" />
		<intercept-url pattern="/generaluser/login" access="isAnonymous()" />
		<intercept-url pattern="/client/register" access="isAnonymous()" />
		<intercept-url pattern="/logout" access="hasAnyRole('ROLE_USER','DA')" />
		<intercept-url pattern="/accessdenied" access="isAnonymous()" />
		<intercept-url pattern="/**" access="hasAnyRole('ROLE_USER','DA')" /> -->
		<form-login login-page="/login" default-target-url="/list"
			authentication-failure-url="/accessdenied" />
		<logout logout-success-url="/logout" />
	</http>
	
	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select username,password, true from generaluser where username=?"
				authorities-by-username-query="select username,'ROLE_USER' from generaluser where username=?" />
		</authentication-provider>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select email as username,password, true from officialuser where email=?"
				authorities-by-username-query="select email as username,privilege as authority from officialuser ou, roles r where ou.roleId = r.roleId and ou.email = ?" />
		</authentication-provider>
	</authentication-manager>

</beans:beans>