<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scheduler.mappers.GeneralUserMapper">

	<select id="getUserRegistrationId" parameterType="int" resultType="String">
		select gcmRegId from generaluser where userId = #{user_id}
	</select>
	<select id="authenticate" parameterType="com.scheduler.models.GeneralUser" resultType="com.scheduler.models.GeneralUser">
		select * from generaluser where username=#{username} and password=#{password}
	</select>
	<select id="getFirstName" parameterType="String" resultType="String">
		select firstName from generaluser where username=#{username} and password=#{password}
	</select>
	<select id="getUserId" parameterType="String" resultType="int">
		select userId from generaluser where username=#{username} and password=#{password}
	</select>
</mapper>