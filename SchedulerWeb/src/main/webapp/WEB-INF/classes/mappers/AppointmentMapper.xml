<?xml58442572584425725844257258442572 version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scheduler.mappers.AppointmentMapper">

	<update id="startAppointmentById" parameterType="int">
		update appointment set
		startTime = CURTIME(),
		officialId = #{official_id},
		meetingFinished = 'N'
		where appointmentId = #{app_id}
	</update>
	
	<select id="getAppointmentById" parameterType="int" resultType="com.scheduler.models.Appointment">
		select * from appointment where appointmentId = #{app_id}
	</select>
	
	<select id="getUserByAppointmentId" parameterType="int" resultType="com.scheduler.models.GeneralUser">
		select gu.* from generaluser gu, 
						 appointment a 
				where 	 a.appointmentId = #{app_id}
				and 	 a.userId = gu.userId
	</select>

</mapper>
